## PENDING SUBMISSION (authorized 2026-06-10)
User authorized a new Arena submission with the R6 goose package. Daily quota was exhausted (resets 2026-06-11T00:00Z = 02:00 CEST). One-shot cron 7f836e94 scheduled 02:09 CEST (SESSION-ONLY — dies if session closes). Manual command if needed:
`PATH="/root/.arena/venv/bin:$PATH" python3 scripts/submit_agent_only.py --tag m27-goose-r6`
Pre-checks already done: arena.yaml defaults = goose/goose_minimax_m3.j2/skills_goose/MAX_ITERATIONS=20 (PR #164), codex OAuth patch disabled, package dry-run = 22 files clean. After submitting: record submission ID in eval-log + README, comment #133/#135, tg ping.
