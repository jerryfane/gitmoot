xer agent runs a ~25min race gate + gets cut off before emitting StructuredOutput → workflow "fails" but the fix IS committed/uncommitted in the worktree; recover by verifying the worktree + gating yourself.
